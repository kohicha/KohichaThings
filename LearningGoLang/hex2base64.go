package main

import(
  "fmt"
)

func main() {
  b := []byte("Hello World")
  fmt.Println(b)
}

// make([]byte, length, capacity)
// _, err := function to run | _ or underscore is a blank identifier, telling the compiler to just run a code block without calling any functions

// Converting Hex to Binary
//
